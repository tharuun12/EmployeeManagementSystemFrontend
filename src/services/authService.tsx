// authService component
import React from 'react';

const authService = () => {
    return (
        <div>
            <h2>authService Page</h2>
        </div>
    );
};

export default authService;
